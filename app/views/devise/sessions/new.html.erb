<!DOCTYPE html>
<html lang="ja">
<header>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        /* ベーススタイル */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background-color: #333;
        }

        .form-container {
            width: 90%;
            max-width: 390px;
            height:100vh;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 0px;
            margin:auto;
            background-color: #1c1c1c;
        }

        h2 {
            font-size: 1.5em;
            margin-bottom: 20px;
            text-align: center;
            color: #f4f4f4;
        }

        h1 {
            text-align: center;
            color: #f4f4f4;
        }

        h3 {
            text-align: center;
            color: #f4f4f4;
        }

        hr {
            margin-bottom:60px;
        }

        /* チェックボックスのスタイル */
        .checkbox-group {
            display: flex;
            flex-direction: column;
            width:90%;
            gap: 10px;
            margin-bottom: 20px;
        }

        label {
            font-size: 1.5em;
            cursor: pointer;
            color: #f4f4f4;
            margin-bottom:20px;
            padding:8px;
        }

        input[type="email"] {
          width: 90%;
          height: 20px;
          margin-top:10px;
          margin-bottom:20px;
        }

        input[type="password"] {
          width: 90%;
          height: 20px;
          margin-top:10px;
          margin-bottom:20px;
        }

        .actions {
          margin-top:20px;
          text-align:center;
        }

        /* app/assets/stylesheets/application.css */
        .custom-submit-btn {
          background-color: #7092ff; /* 背景色 */
          color: white;             /* 文字色 */
          padding: 10px 20px;       /* 内側の余白 */
          border: none;             /* 枠線なし */
          border-radius: 5px;       /* 角丸 */
          font-size: 16px;          /* 文字サイズ */
          cursor: pointer;          /* カーソルをポインタに */
        }

        /* ボタンにホバー効果を追加 */
        .custom-submit-btn:active {
          background-color: #7370ff;
        }


        @media (max-width: 600px) {
            .form-container {
                padding: 15px;
                max-width: 100%; /* 画面幅いっぱいに広げる */
            }

            .submit {
                font-size: 14px; /* ボタンの文字サイズを小さくする */
            }
        }

    </style>
</header>
<body>
<div class="form-container">
  <header>
    <h1>PAシート</h1>
    <h3>冬定オーデ2024</h3>
    <hr color='#3c3c3c'>
  </header>
  <h2>ログイン</h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <div class="label"><%= f.label :email ,"メールアドレス"%></div>
      <div class="text"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
    </div>

    <div class="field">
      <div class="label"><%= f.label :password ,"パスワード"%></div>
      <div class="text"><%= f.password_field :password, autocomplete: "current-password" %></div>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="checkbox">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me ,"ログイン状態を保持" %>
      </div>
    <% end %>

    <div class="actions">
      <%= f.submit "ログイン" ,class: "custom-submit-btn" %>
    </div>
  <% end %>
</div>
</body>
</html>

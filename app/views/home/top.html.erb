<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>所属バンド選択</title>
    <style>
        /* ベーススタイル */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background-color: #333;
        }

        .form-container {
            width: 90%;
            max-width: 400px;
            min-height: 100vh;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 0px;
            margin:auto;
            background-color: #1c1c1c;
        }

        h2 {
            font-size: 1.5em;
            margin-bottom: 10px;
            text-align: center;
            color: #f4f4f4;
        }

        h1 {
            text-align: center;
            color: #f4f4f4;
        }

        h3 {
            text-align: center;
            color: #f4f4f4;
            padding-bottom:10px;
        }

        hr {
            margin-bottom:60px;
        }

        .instruction {
            margin-bottom:40px;
        }

        label {
            display: block;
            margin-top: 10px;
            font-size: 0.9rem;
            color: #555;
        }
        .posts-container {
            display: flex;
            align-items: center;
            flex-direction: column;
            gap: 20px;
        }
        .posts-index-item {
            display: flex;
            align-items: center;
            background-color: #3c3c3c;  /* カードの暗めの背景色 */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            text-decoration: none;
            color: inherit;
            width: 80%;
        }
        .posts-index-item:active{
            transform: translateY(-5px);
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
        }
        .posts-index-item:active li {
            color:#7092ff;
        }
        .posts-index-item li {
            list-style: none;
            margin-right: 30px;
            font-size: 1.2rem;
            color: #f4f4f4;  /* カード内テキストも明るめのグレー */
        }


    </style>
</head>
<body>

<div class="form-container">
    <header>
        <h1>PAシート</h1>
        <h3>冬定オーデ2024</h3>
        <h2>1/2</h2>
        <hr color='#3c3c3c'>
    </header>
    <h2 class="instruction">所属バンドを以下から<br>選択してください
    </h2>
    <div class="posts-container">
        <% @bands.each do |band| %>
            <a href='<%="/post/#{band.bandid}"%>' class="posts-index-item" data-turbo="false">
                <li class='livename'><%= band.bandname %></li>
            </a>
        <% end %>
    </div>
</div>

</body>
</html>
